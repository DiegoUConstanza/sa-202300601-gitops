apiVersion: v1
kind: Service
metadata:
  name: {{ include "progreso-service.name" . }}
  labels:
    {{- include "progreso-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "progreso-service.selectorLabels" . | nindent 4 }}
